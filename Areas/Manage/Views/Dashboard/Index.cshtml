
@{
    ViewData["Title"] = "Index";
}

<!-- partial -->
@model ICollection<Product>
<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Image</th>
                <th>Price</th>
            </tr>
        </thead>
        <body>
        
        <a href="./Category">
        <td>1</td>
        <td>1</td>
        <td><img width="40" src="" /></td>
        <td>100</td>
        <td><a class="btn btn-danger" asp-area="Manage" asp-controller="Category" asp-action="Delete" asp-route-id="11">Delete</a></td>
        <td><a class="btn btn-info" asp-area="Manage" asp-controller="Category" asp-action="Update" asp-route-id="1">Update</a></td>
        </a>
        </body>
        @*<body>
            @foreach (var item in Model)
            {
                <a href="./Category">
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td><img width="40" src="@item.Imageurl" /></td>
            <td>@item.price</td>
            <td><a class="btn btn-danger" asp-area="Manage" asp-controller="Category" asp-action="Delete" asp-route-id="@item.Id">Delete</a></td>
            <td><a class="btn btn-info" asp-area="Manage" asp-controller="Category" asp-action="Update" asp-route-id="@item.Id">Update</a></td>
            </a>
            }
        </body>*@
    </table>
</div>